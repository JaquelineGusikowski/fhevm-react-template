{
  "name": "farmer-subsidy-fhe",
  "version": "1.0.0",
  "description": "Anonymous Farmer Subsidy System with FHE",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia",
    "serve": "npx http-server public -p 1888 -c-1 --cors"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@fhevm/solidity": "latest"
  }
}
